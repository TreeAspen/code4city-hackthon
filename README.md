
  # Dashboard prototype for 311 data

  This is a code bundle for Dashboard prototype for 311 data. The original project is available at https://www.figma.com/design/mMZ8S1N2PU940eVBokP7fM/Dashboard-prototype-for-311-data.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  